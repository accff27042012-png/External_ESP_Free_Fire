name: Build Dylib

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Hack Dylib from Core/
        run: |
          mkdir -p output
          
          # Kiểm tra xem có thư mục Core/ không
          if [ ! -d "Core" ]; then
            echo "ERROR: Không tìm thấy thư mục Core/"
            exit 1
          fi
          
          # Tìm tất cả file .mm và .cpp
          SOURCES=$(find Core -name "*.mm" -o -name "*.cpp" | tr '\n' ' ')
          
          if [ -z "$SOURCES" ]; then
            echo "ERROR: Không tìm thấy file source nào trong Core/"
            exit 1
          fi
          
          echo "Compiling: $SOURCES"
          
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          
          clang++ -arch arm64 -dynamiclib \
            -isysroot "$SDK_PATH" \
            -framework UIKit \
            -framework Foundation \
            -framework CoreGraphics \
            -framework QuartzCore \
            -framework OpenGLES \
            -framework Metal \
            -framework CoreImage \
            $SOURCES \
            -o output/UltimateHack.dylib \
            -install_name @rpath/UltimateHack.dylib \
            -fobjc-arc \
            -std=c++14 \
            -O3 \
            -Wno-deprecated-declarations \
            -Wno-unused-variable
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: UltimateHack
          path: output/*.dylib
